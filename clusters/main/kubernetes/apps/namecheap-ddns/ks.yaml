apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: namecheap-ddns
  namespace: flux-system
spec:
  interval: 10m
  path: clusters/main/kubernetes/apps/namecheap-ddns/app
  prune: true
  sourceRef:
    kind: GitRepository
    name: cluster

